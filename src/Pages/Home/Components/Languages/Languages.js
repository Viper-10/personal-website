import BarChart from "./components/BarChart/BarChart";

const Languages = () => {
  return (
    <div class="skills">
      <div class="heading">Languages</div>
      {/* TODO: add line chart and bar chart symbol */}

      {<BarChart />}
    </div>
  );
};

export default Languages;
